services:
  api:
    build: ../../services/api
    env_file:
      - ../../.env
    ports: [ "8000:8000" ]
    command: uvicorn main:app --host 0.0.0.0 --port 8000
  frontend:
    build: ../../frontend/dapp
    env_file:
      - ../../.env
    ports: [ "3000:3000" ]
    command: npm run dev
